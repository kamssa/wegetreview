import { RefoidirOmelettePipePipe } from './refoidir-omelette-pipe.pipe';

describe('RefoidirOmelettePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RefoidirOmelettePipePipe();
    expect(pipe).toBeTruthy();
  });
});
